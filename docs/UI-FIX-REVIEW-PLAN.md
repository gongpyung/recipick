# UI Fix Review Plan — Playwright 이슈 수정 검토

## 검토 목표

UI-FIX-PLAN.md의 5건 수정이 올바르게 적용되었는지 Playwright로 시각적 검증.

---

## Fix 1: 재료명 줄바꿈

### 데스크톱 (1440px)

- [ ] 레시피 결과 화면에서 재료명이 단어 단위로 줄바꿈되는가 (한 글자씩 ❌)
- [ ] "통마늘", "청양고추" 등이 정상 표시되는가
- [ ] 수량 뱃지(핑크 배경)가 잘리지 않는가
- [ ] `break-keep` 클래스가 적용되었는가
- [ ] `flex-1`이 이름 영역 div에 적용되었는가

### 모바일 (375px)

- [ ] 동일하게 재료명이 단어 단위 줄바꿈인가
- [ ] 긴 재료명(예: "돼지고기 앞다리살")이 정상 표시되는가

---

## Fix 2: 데스크톱 하단 네비 숨기기

### 데스크톱 (1440px)

- [ ] 하단 네비게이션이 **보이지 않는가**
- [ ] 콘텐츠가 하단 네비에 가려지지 않는가
- [ ] 헤더 네비게이션("최근 레시피")은 정상 표시되는가
- [ ] 하단 여백이 `md:pb-0` 또는 `md:pb-8`로 적절한가

### 모바일 (375px)

- [ ] 하단 네비게이션이 **정상 표시되는가**
- [ ] 5개 탭 모두 보이는가 (홈/레시피/검색/즐겨찾기/설정)
- [ ] `pb-24`로 콘텐츠가 네비에 가려지지 않는가

### 확인 페이지

- [ ] 홈 (`/`)
- [ ] 레시피 결과 (`/recipes/:id`)
- [ ] 히스토리 (`/history`)

---

## Fix 3: 레시피 페이지 너비

### 데스크톱 (1440px)

- [ ] 레시피 콘텐츠가 화면 중앙에 적절한 너비로 표시되는가
- [ ] 재료 + 조리순서 2컬럼 그리드가 정상 동작하는가
- [ ] `lg:max-w-4xl` (896px) 적용 확인

### 모바일 (375px)

- [ ] 기존 `max-w-2xl` 동작 유지 (변경 없음)

---

## Fix 4: 히스토리 페이지 너비

### 데스크톱 (1440px)

- [ ] 히스토리 카드가 3컬럼 그리드로 표시되는가
- [ ] `lg:max-w-4xl` 적용 확인
- [ ] 카드들이 적절한 크기로 표시되는가

### 모바일 (375px)

- [ ] 기존 `max-w-lg` 동작 유지 (단일 컬럼)

---

## Fix 5: 모바일 hero 패딩

### 모바일 (375px)

- [ ] hero 섹션 하단과 피처 카드 사이 간격 적절
- [ ] 피처 카드가 하단 네비에 가려지지 않음

---

## 자동 검증

- [ ] `npm run type-check` 에러 0건
- [ ] `npm run lint` 에러 0건
- [ ] `npm run build` 성공

---

## Playwright 검증 명령 (검토자용)

```
1. 데스크톱(1440x900)으로 리사이즈
2. /recipes/:id 접속 → 풀페이지 스크린샷
3. 모바일(375x812)로 리사이즈
4. /recipes/:id 접속 → 풀페이지 스크린샷
5. 데스크톱(1440x900)으로 / 접속 → 풀페이지 스크린샷
6. 데스크톱(1440x900)으로 /history 접속 → 풀페이지 스크린샷
```

---

## 판정 기준

| 등급           | 기준                                                   |
| -------------- | ------------------------------------------------------ |
| ✅ PASS        | 5건 모두 수정 + Playwright 스크린샷에서 이슈 해소 확인 |
| ⚠️ CONDITIONAL | 4건 이상 수정 + 재료 줄바꿈(Fix 1) 해소 확인           |
| ❌ FAIL        | Fix 1 또는 Fix 2 미적용, 또는 회귀 발생                |
